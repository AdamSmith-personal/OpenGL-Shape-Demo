This project was developed to work on Linux systems. OpenGL is just a specification and how its installed is different on every operating system.
It can be installed only on linux systems by opening the project in the terminal and typing:

sudo chmod +x install.sh
./install.sh
